---
label: Authors
hide_body: true
fields:
- name: authors_name
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: author-list
      file: content/data/authors.json
  label: Authors Name
