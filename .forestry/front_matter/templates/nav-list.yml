---
label: Nav List
hide_body: true
fields:
- name: main_nav
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: articles
  label: Main Nav
- name: list
  type: blocks
  label: List
  template_types:
  - nav-list
  config:
    min: 
    max: 
